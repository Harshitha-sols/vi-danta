<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name="androidx.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VDanta"
        tools:targetApi="31">

        <!-- Welcome Activity (Launcher) -->
        <activity
            android:name=".Welcome"
            android:exported="true"
            android:theme="@style/Theme.VDanta">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Welcome1 Activity -->
        <activity android:name=".Welcome1" android:exported="true" />

        <!-- Signup Activity -->
        <activity android:name=".SignupActivity" android:exported="true" />

        <!-- Signin Activity -->
        <activity android:name=".SignInActivity" android:exported="true" />


        <activity android:name=".DashBoardActivity"/>
        <activity android:name=".GeneralDentistryActivity"/>
        <activity android:name=".CosmeticDentistryActivity"/>
        <activity android:name=".OrthodonticsActivity"/>
        <activity android:name=".OralSurgeryActivity"/>
        <activity android:name=".PediatricDentistryActivity"/>
        <activity android:name=".PeriodonticsActivity"/>
        <activity android:name=".RestorativeDentistryActivity"/>
        <activity android:name=".EmergencyDentalCareActivity"/>

        <activity android:name=".BookingActivity" />



    </application>

</manifest>
